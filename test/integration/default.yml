- hosts: all
  roles:
    - ansible-role-logstash
  vars:
    logstash_inputs: |
      tcp {
        'port' => '5140'
        'type' => 'syslog'
      }
    logstash_outputs: |
      elasticsearch {}
